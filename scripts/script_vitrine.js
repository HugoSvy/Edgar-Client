document.addEventListener('DOMContentLoaded', function () {
    AOS.init({
        duration: 1000, // durée de l'animation en millisecondes
        once: true // si `true`, l'animation s'exécute une seule fois
    });
});
